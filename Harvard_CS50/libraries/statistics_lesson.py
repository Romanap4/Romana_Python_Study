# mean() is a function in the statistics library that's quite useful

import statistics

print(statistics.mean([100, 90]))

# the mean() function takes a list of values and prints out the average of these values
